apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: DS_POSTGRESQL
    type: postgres
    access: proxy
    isDefault: true

    url: postgres:5432

    user: secu
    database: securitydb

    secureJsonData:
      password: secret

    jsonData:
      sslmode: disable
      postgresVersion: 1600